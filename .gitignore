# Build output
target/

# Local secrets — use GitHub Actions secrets in CI
config.properties

# Local/generated dated resume copies
*.pdf
!resume/*.pdf

# Selenium failure dumps from CI/local
debug/

# Saved Naukri login session (secret — never commit)
naukri-cookies.json

# IDE / OS
.idea/
*.iml
.vscode/
.DS_Store
